// Deployed proxy/worker.js — the one Cloudflare Worker backing both Ask Xabi
// (Gemini passthrough) and My Team's FPL import (/fpl/<path> passthrough,
// since fantasy.premierleague.com sends no CORS headers of its own).
// Empty string = not deployed yet. See proxy/README.md for the deploy steps
// and the exact URL to paste in here once it exists.
export const PROXY_URL = '';
